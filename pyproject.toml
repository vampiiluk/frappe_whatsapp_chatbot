[project]
name = "frappe_whatsapp_chatbot"
authors = [
    { name = "Shridhar Patil", email = "shrip.dev@gmail.com"}
]
description = "WhatsApp Chatbot for Frappe with keyword replies, conversation flows, and optional AI"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[project.optional-dependencies]
ai = [
    "openai>=1.0.0",
    "anthropic>=0.18.0",
    "google-generativeai>=0.5.0"
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=14.0.0,<=17.0.0-dev"
frappe_whatsapp = ">=1.0.0,<2.0.0"
